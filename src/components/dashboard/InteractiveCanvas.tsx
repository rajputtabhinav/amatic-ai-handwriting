"use client";

/**
 * Interactive Canvas - Not needed anymore
 * All functionality is now in canvas-drawing.tsx
 * Keeping this as a placeholder to prevent import errors
 */

import { forwardRef } from 'react';

export const InteractiveCanvas = forwardRef(() => {
  return null;
});

InteractiveCanvas.displayName = 'InteractiveCanvas';
