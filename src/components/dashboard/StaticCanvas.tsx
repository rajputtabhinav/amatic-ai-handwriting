"use client";

/**
 * Static Canvas - Not needed anymore
 * All functionality is now in canvas-drawing.tsx
 * Keeping this as a placeholder to prevent import errors
 */

import { memo } from 'react';

export const StaticCanvas = memo(() => {
  return null;
});

StaticCanvas.displayName = 'StaticCanvas';
