import { Viewport } from 'next';

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 3,
  minimumScale: 0.5,
  userScalable: true,
  viewportFit: 'cover',
  themeColor: '#6366F1'
};
